# @swan-io packages (lake, shared-business, use-form) are published to the public registry.
# Pin the scope here so a contributor's personal GitHub Packages mapping doesn't bake
# npm.pkg.github.com tarball URLs into the lockfile, which CI can't authenticate against.
@swan-io:registry=https://registry.npmjs.org/
